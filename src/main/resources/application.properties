spring.application.name=School
spring.datasource.url=jdbc:h2:mem:demo;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# Your database URL already includes DB_CLOSE_ON_EXIT=FALSE, which prevents the database from closing when the JVM exits.
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.show-sql=true

server.port=8080

jwt.secret=this-is-a-demo-secret-change-me-please-noneedtodecodemeusemeasitis
jwt.access-token-validity-ms=900000   # 15 minutes
jwt.refresh-token-validity-ms=1209600000 # 14 days
jwt.expiration=86400000

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
